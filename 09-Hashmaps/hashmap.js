class HashMap{
    constructor(size){
        this.map = new Array(size)  //Hashmap of fixed size
        this.size = size
    }

    //Hash Function[ hash(key) ]:accept string key, convert to hashcode, map into an index

    //Set(key,value) to store key:value pair

    //Get(key) to retrieve a value based on its key

    //Remove(key) to delete a key:value pair

    //print()
}

const map = new HashMap(50)